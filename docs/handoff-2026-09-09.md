# Handoff prompt — 2026-09-09

Paste everything below the line into a fresh Claude session to continue where the
last one stopped. It contains no secrets and is safe to commit.

---

CONTEXT HANDOFF — Healing Partners / Remember Them
Repo: /Users/brandon/Projects/Healing-Partners (macOS, zsh). Node v24 at ~/.local/bin (leave it; a Homebrew node was installed and removed). Xcode is installed but its license is NOT accepted, so /usr/bin/python3 currently refuses to run — don't use python3 for anything; the test harness was switched to a Node server for this reason.

WHAT HAS BEEN DONE (all uncommitted in the working tree unless noted)
1. A security/honesty review of the site was completed. The important findings are all now fixed or tracked below. See docs/log-2026-09-08.md for the record.
2. Block 1 (copy honesty) — DONE locally, NOT pushed to the Shopify store:
   - remember-them/designer.html paywall now states the six-month term / $350 alternative instead of "cancel any time".
   - pay.html, pricing.html, for-funeral-homes.html, shopify/hp-home.liquid, shopify/for-funeral-homes.page.html: the $350 plan now says "cancel any month with ten days' notice" (matching the Stripe checkout page), "nothing is deleted" became "exportable for 30 days, deleted within 90", the $350 link is stated to have no trial, "VR at no extra cost" became "$50 3D-model add-on", privacy commitments aligned across pages, "card required" added to the Shopify page.
   - shopify/for-funeral-homes.body.html regenerated with the README's awk command. `node shopify/check-drift.js /tmp/live.html` (after `curl -s https://healingpartners.us/pages/for-funeral-homes > /tmp/live.html`) will list the six new sentences as not yet live — expected until pushed.
3. Block 2 (money/data paths) — DONE and DEPLOYED:
   - supabase/migrations/0005_close_review_findings.sql is APPLIED on project zhtjgigkgpzrzeaqjwsv (migration list shows 0001–0005 local=remote). It makes the commission/stale-design views obey RLS, adds a guard trigger so browsers can't write vr_status=paid / insured / commission_* columns, revokes release_blockers() from anon, adds a 'recapture' payment event type, and adds model_calls + allow_model_call(kind, per_hour) for rate-limiting.
   - supabase/functions/stripe-webhook (no longer 500s on non-UUID client_reference_id; books the $200 recapture as 'recapture'; captures checkout email/name), interview and classify-story (require a JWT with sub, call allow_model_call, slice before redact) are DEPLOYED. Probing the URLs: stripe-webhook → 400 (JWT check off, as intended), interview/classify-story/media-sign → 401.
   - supabase/config.toml created via `supabase init`; per-function verify_jwt pinned (false for stripe-webhook, true for the rest). CLI is logged in and linked from the repo root; the access token used for login has since been deleted, so a fresh `supabase login` may be needed before any CLI write.
4. Mobile test harness — DONE, 420/420 passing: playwright-mobile-demo/ (14 device/browser projects, tests/hub.spec.js + tests/remember-them.spec.js, node static server tests/serve.cjs, sim/run.sh for the iOS Simulator). Run: `cd playwright-mobile-demo && npx playwright test`. It found and fixed: designer size <select> was 13px on phones (iOS zoom) and several sub-44px touch targets (CSS added under @media (pointer:coarse) in index.html, remember-them/designer.html, remember-them/index.html, apps/life-interview/index.html).
5. A `supabase-server` skill was installed (.agents/skills/supabase-server, docs only). It recommends migrating the four edge functions from esm.sh/createClient + anon/service-role keys to `withSupabase({ auth: 'user' | 'none' })` — a follow-up refactor, not done.

CREDENTIAL HYGIENE (important)
During the last session three credentials were pasted into a chat window: the database password, an sb_secret_ key, and an sbp_ access token. Brandon confirmed on 2026-09-09 that all three have been deactivated, so no rotation is outstanding. They are dead keys, which means a fresh `supabase login` is needed before any CLI write, and the function secrets in Step 1 below must be set from newly issued keys rather than the ones used last session. Never paste secrets into chat; set them in the Terminal only. The auto-mode classifier blocks any command containing a credential and blocks `supabase` CLI/connector calls once secrets have appeared in the conversation — so keep secrets out of the conversation entirely.

WHERE WE STOPPED — `supabase secrets list` shows ONLY platform-managed secrets. None of the app secrets exist. Next steps, in order:

STEP 1 (Brandon, Terminal): set the function secrets
  supabase secrets set STRIPE_SECRET_KEY=sk_live_...        # Stripe → Developers → API keys (live, because pay.html's links are live links)
  supabase secrets set ANTHROPIC_API_KEY=sk-ant-...         # console.anthropic.com → API Keys
  supabase secrets set ALLOWED_ORIGINS=https://healingpartners.us,https://brandonjameshomer-ship-it.github.io
  (later, for photo upload) supabase secrets set R2_ACCOUNT_ID=... R2_ACCESS_KEY_ID=... R2_SECRET_ACCESS_KEY=... R2_BUCKET=...
  then `supabase secrets list` to confirm.

STEP 2 (Brandon, Stripe dashboard): Developers → Webhooks → Add endpoint
  URL: https://zhtjgigkgpzrzeaqjwsv.supabase.co/functions/v1/stripe-webhook
  Events: checkout.session.completed, checkout.session.expired, checkout.session.async_payment_failed, customer.subscription.created, customer.subscription.updated, customer.subscription.trial_will_end, customer.subscription.deleted, invoice.paid, invoice.payment_failed, charge.refunded
  Copy the signing secret → `supabase secrets set STRIPE_WEBHOOK_SECRET=whsec_...`
  Then "Send test webhook" → expect 200. (400 = signing secret not set; 500 = handler error, paste the body to Claude.)

STEP 3 (needs Brandon's explicit yes): push Block 1 to the live store — pageUpdate of shopify/for-funeral-homes.body.html to the Shopify page (gid://shopify/Page/137389080794) and upload shopify/hp-home.liquid as snippets/hp-home.liquid to the theme. Then re-run check-drift.js to confirm it's green.

STEP 4: commit the working tree in sensible groups (block 1 copy; block 2 backend + migration + config; Playwright harness; touch-target CSS; skill files; these docs). Nothing has been committed by Claude; another session has been committing separately, so `git status` first.

OPEN DECISIONS for Brandon:
  a) "ten days' notice" on the $350 plan: keep it in the copy (it's what Stripe's checkout shows, "See User Agreement") or remove it from the Stripe product description if Sec. 5.1 doesn't actually say it.
  b) Reconcile dashboard/pricing.sql with migration 0003 (markup vs retail_markup, two insurance column sets, memorial_sale_price function vs column) — a schema-design choice.
  c) The browser-only "trial" in remember-them/designer.html (Access.state(), localStorage, ?unlock=1) is not enforcement and is never linked to a Stripe subscription; either wire Stripe → unlock or stop calling it a trial.
  d) Whether to do the @supabase/server refactor of the four edge functions.

BLOCK 3 (after the above): app.healingpartners.us CNAME per SHOPIFY.md; run the Playwright suite in GitHub Actions on every push; finish the iOS Simulator setup (`sudo xcodebuild -license accept`, `sudo xcodebuild -runFirstLaunch`, `xcodebuild -downloadPlatform iOS`) then `playwright-mobile-demo/sim/run.sh` for real Safari screenshots; photograph the granite samples (colours on the site are drawn, and the pages say so).

RULES FOR THIS SESSION: don't run git commit/push unless asked; don't paste or echo any secret; ask before any production write (Shopify push, Stripe changes, DB migrations); verify claims against the code rather than the summaries above.
